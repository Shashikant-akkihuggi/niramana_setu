name: untitled3
description: "Niramana Setu – Construction Project Management App"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.1

  # 🔥 Firebase
  firebase_core: ^3.5.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.4.0
  cloud_functions: ^5.1.0
  google_sign_in: ^6.2.1

  # 📦 Offline & Utilities
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^6.0.5
  uuid: ^4.4.0
  webview_flutter: ^4.8.0

  # 🎨 SVG Support
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  # ✅ Asset declaration (FOLDER LEVEL – IMPORTANT)
  assets:
    - assets/images/

# ✅ App Icon Configuration
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/icon.png"
